apiVersion: v1
kind: Service
metadata:
  name: nodejs-k8s-cicd-service
  namespace: nodejs-cicd
spec:
  type: NodePort
  selector:
    app: nodejs-k8s-cicd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080